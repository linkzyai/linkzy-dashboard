# Blog index should be handled by React (NOT the SSR function)
/blog    /index.html   200
/blog/   /index.html   200

# Blog detail pages go to the SSR function (always passes slug via :splat)
/blog/*  /.netlify/functions/blog-ssr/:splat  200

# Everything else to React SPA
/*       /index.html   200

